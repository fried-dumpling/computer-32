#define stack_val (4*100)
#define data_val 0xFFFFFFFF
#define loop_count 10

.text
	set stack.S16H, stack_val >> 16
	set stack.16L, stack_val
	set gen[0].S16H, data_val >> 16
	set gen[0].16L, data_val

loop:
	push gen[0].32B
	jmp.one zero.32B, loop

	halt